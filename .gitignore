# Byte-compiled / optimized / DLL files
__pycache__/
coverage.xml
.coverage

# Virtual environments
venv/

# Secrets
secrets/

# Backups
backups/

# LLM categorization cache (regenerated at runtime)
data/llm_category_cache.json

# Gemini
GEMINI.md

# Personal settings (copy settings.example.json → settings.json and fill in)
settings.json

# Personal categorization rules (copy data/matchers.example.json → data/matchers.json)
data/matchers.json

# Personal Gmail fetcher config (copy settings.example.json → settings.json)
plugins/gmail_fetcher/settings.json

# Personal salary splitter config (copy settings.example.json → settings.json)
plugins/salary_splitter/settings.json

# Personal telegram bot config (copy settings.example.json → settings.json)
plugins/telegram_bot/settings.json

# Personal ignore rules (copy data/ignore.example.json → data/ignore.json)
data/ignore.json
